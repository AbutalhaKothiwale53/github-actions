import App from '../App';

describe('App', () => {
  it('should render', () => {
    expect(App).toBeDefined();
  });
});

// Need to add test and code coverage
// Need to add test and code coverage
// Need to add test and code coverage
// Need to add test and code coverage